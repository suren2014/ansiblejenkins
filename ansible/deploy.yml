---
- name: Jenkins Ansible Deployment
  hosts: all
  become: true

  tasks:
    - name: Run Docker container
      docker_container:
        name: web
        image: nginx
        tag: latest
        tty: true
        state: started
        ports:
          - "80:80"
